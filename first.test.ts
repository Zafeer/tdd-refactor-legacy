import { describe, expect, it } from 'vitest';

describe('first test', () => {
  it('1 should be 1', () => {
    expect(1).toBe(1);
  });

  it('1 should be 1', () => {
    expect(1).toBe(1);
  });
});
